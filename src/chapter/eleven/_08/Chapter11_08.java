package chapter.eleven._08;

public class Chapter11_08 {

  /** ◆ ArrayList 의 메서드
   *
   * 1. ArrayList(): 크기가 0인 ArrayList 를 생성
   * 2. ArrayList(Collection c): 주어진 컬렉션이 저장된 ArrayList 생성
   * 3. ArrayList(int initialCapacity): 지정된 초기용량을 갖는 ArrayList 생성
   * 4. boolean add(Object o): ArrayList 의 마지막에 객체를 추가. 성공하면 true
   * 5. void add(int index, Object element): 지정된 위치(index) 에 객체를 저장
   * 6. boolean addAll(Collection c): 주어진 컬렉션의 모든 객체를 저장한다.
   * 7. boolean addAll(int index, Collection c): 지정된 위치부터 주어진 컬렉션의 모든 객체를 저장한다.
   * 8. void clear(): ArrayList 를 완전히 비운다.
   * 9. Object clone(): ArrayList 를 복제한다.
   * 10. boolean contains(Object o): 지정된 객체(o) 가 ArrayList 에 포함되어 있는지 확인
   * 11. void ensureCapacity(int minCapacity): ArrayList 의 용량이 최소한 minCapacity 가 되도록 한다.
   * 12. Object get(int index): 지정된 위치(index) 에 저장된 객체를 반환한다.
   * 13. int indexOf(Object o): 지정된 객체가 저장된 위치를 찾아 반환한다.
   * 14. boolean isEmpty(): ArrayList 가 비어있는지 확인한다.
   * 15. Iterator iterator(): ArrayList 의 Iterator 객체를 반환한다.
   * 16. int lastIndexOf(Object o): 객체(o) 가 저장된 위치를 역방향순으로 검색해서 반환
   * 17. ListIterator listIterator(): ArrayList 의 ListIterator 를 반환한다.
   * 18. ListIterator listIterator(int index): ArrayList 의 지정된 위치부터 시작하는 ListIterator 를 반환한다.
   * 19. Object remove(int index): 지정한 위치(index)에 있는 객체를 제거한다.
   * 20. boolean remove(Object o): 지정한 객체를 제거한다.
   * 21. boolean removeAll(Collection c): 지정한 컬렉션에 저장된 것과 동일한 객체들을 ArrayList 에서 제거한다.
   * 22. boolean retainAll(Collection c): ArrayList 에 저장된 객체 중에서 주어진 컬렉션과 공통된 것들만 남기고 나머지는 다 제거한다.
   * 23. Object set(int index, Object element): 주어진 객체(element) 를 지정된 위치(index)에 저장한다.
   * 24. int size(): ArrayList 에 저장된 객체의 개수를 반환한다.
   * 25. void sort(Comparator c): 지정된 정렬기준(c) 으로 ArrayList 를 정렬한다
   * 26. List subList(int fromIndex, int toIndex): fromIndex 부터 toIndex 사이에 저장된 객체를 반환한다.
   * 27. Object[] toArray(): ArrayList 에 저장된 모든 객체들을 객체배열로 반환한다.
   * 28. Object[] toArray(Object[] a): ArrayList 에 저장된 모든 객체들을 객체배열 a에 담아 반환한다.
   * 29. void trimToSize(): 용량을 크기에 맞게 줄인다.(빈 공간을 없앤다)
   *
   *
   *
   *
   * */

}
