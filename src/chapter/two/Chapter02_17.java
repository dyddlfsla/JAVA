package chapter.two;

public class Chapter02_17 {

  /** ◆ 타입 간의 변환 방법
   *
   * 타입 간의 변환은 정리해서 알아두면 좋다.
   *
   *  1. 숫자를 문자로 변환 → 숫자에 '0'을 더한다.
   *  2. 문자를 숫자로 변환 → 문자에서 '0'을 뺀다.
   *  3. 숫자를 문자열로 변환 → 빈 문자열 ""을 더한다.
   *  4. 문자열을 숫자로 변환 → Integer.parseInt() 또는 Double.parseDouble()를 활용.
   *  5. 문자열을 문자로 변환 → charAt(0)을 사용한다.
   *  6. 문자를 문자열로 변환 → 빈 문자열 ""을 더한다.
   *
   * */

}
